import { CommandOptions } from '../types';

export async function searchCommand(query: string, options: CommandOptions) {
  console.log('Search command - to be implemented');
  console.log(`Query: ${query}`);
  console.log(`Options:`, options);
} 